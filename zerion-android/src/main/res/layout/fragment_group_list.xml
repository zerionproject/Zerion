<?xml version="1.0" encoding="utf-8"?>
<!-- Private Groups list with floating invitation banner -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- Main list -->
    <com.professor.zerion.android.view.ZerionRecyclerView
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
        app:scrollToEnd="false" />

    <!-- FAB for adding new group -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_group"
        style="@style/Zerion.FAB"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:contentDescription="@string/groups_button_add"
        app:srcCompat="@drawable/ic_add_white" />

    <!-- Floating invitation banner - above FAB -->
    <include
        android:id="@+id/invitation_banner"
        layout="@layout/group_invitation_banner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="80dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
