<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:animateLayoutChanges="true"
	tools:showIn="@layout/activity_conversation">

	<!-- Modern floating input bar - no divider needed -->
	<LinearLayout
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:background="@android:color/transparent"
		android:paddingStart="8dp"
		android:paddingEnd="4dp"
		android:paddingTop="8dp"
		android:paddingBottom="8dp"
		android:gravity="center_vertical"
		android:orientation="horizontal">

		<com.professor.zerion.android.view.EmojiTextInputView
			android:id="@+id/emojiTextInput"
			android:layout_width="0dp"
			android:layout_height="wrap_content"
			android:layout_weight="1"
			android:layout_marginEnd="4dp"
			app:maxTextLines="6" />

		<androidx.appcompat.widget.AppCompatImageButton
			android:id="@+id/secretNoteButton"
			android:layout_width="36dp"
			android:layout_height="36dp"
			android:layout_marginEnd="2dp"
			android:layout_gravity="center_vertical"
			android:background="?attr/selectableItemBackgroundBorderless"
			android:contentDescription="@string/secret_note_button_desc"
			android:padding="6dp"
			android:scaleType="center"
			app:srcCompat="@drawable/ic_shield_lock"
			app:tint="@color/zerion_cyan" />

		<com.professor.zerion.android.view.CompositeSendButton
			android:id="@+id/compositeSendButton"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:layout_gravity="center_vertical" />

	</LinearLayout>

</merge>
