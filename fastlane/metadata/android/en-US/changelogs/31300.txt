Tor updated to 0.4.9.13 (the September 2026 security release), now built from the signed upstream source in the project and verified against a recorded hash before it runs. Client authorization credentials are re-installed after every network change, so locked-in contacts stay reachable without restarting Tor. Tor process handling is more robust and the network screen reports publication from Tor itself.
