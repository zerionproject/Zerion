Idle data usage cut five to ten times: cover traffic now has an active and an idle rate (slower on mobile data, with a setting); real and cover frames stay indistinguishable within each rate. Storage cleanup for cancelled uploads and orphaned attachment chunks; the database compacts itself. Vault auto-lock and hide-content settings work as configured. Crash fix in chats with voice-call history and many smaller fixes.
