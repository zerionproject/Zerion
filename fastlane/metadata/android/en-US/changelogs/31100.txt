Fixes a bug where the app could stay offline to contacts after a spell without signal until it was force closed; the app now notices when a link stops passing traffic and restarts Tor's network when it is stuck reconnecting. Upgrades Tor to 0.4.9.12. The Monero wallet library is built from a clean tree on every release with recorded hashes so the F-Droid build can be verified against the published APK.
